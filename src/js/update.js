function pageInit(){
    const key = sessionStorage.getItem('key');
    console.log(key)
    // Read in the object from the realtime database with that key
    // put data into form to be updated
}

pageInit()